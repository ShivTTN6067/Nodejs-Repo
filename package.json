{
  "name": "PracticeNodeTS",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "jest": {
    "verbose": true
  },
  "scripts": {
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "test": " ts-mocha --exit **/test/*.spec.ts",
    "test-with-coverage": "nyc --reporter=html --reporter=text ts-mocha --exit **/test/*.spec.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/expect": "^24.3.0",
    "@types/express": "^4.17.14",
    "@types/mocha": "^10.0.0",
    "mocha": "^10.0.0",
    "nodemon": "^2.0.20",
    "nyc": "^15.1.0",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "api": "^4.5.2",
    "chai": "^4.3.6",
    "dotenv": "^16.0.3",
    "express": "^4.18.1",
    "supertest": "^6.3.0"
  }
}
